# stem Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-04-17

## Active Technologies

- Java 17 + Spring Boot `3.2.5` from the root `pom.xml`, Spring Framework via the Boot BOM, Maven multi-module reactor, planned `spring-boot-starter-data-jpa`/Hibernate in `stem-core` for relational persistence, Microsoft SQL Server JDBC driver for Azure SQL, `spring-boot-starter-web` and validation support in `stem-api`, and Azure Event Hubs client support in `stem-spring-boot-starter` only when Event Hub publishing is enabled (001-human-task-engine)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for Java 17

## Code Style

Java 17: Follow standard conventions

## Recent Changes

- 001-human-task-engine: Added Java 17 + Spring Boot `3.2.5` from the root `pom.xml`, Spring Framework via the Boot BOM, Maven multi-module reactor, planned `spring-boot-starter-data-jpa`/Hibernate in `stem-core` for relational persistence, Microsoft SQL Server JDBC driver for Azure SQL, `spring-boot-starter-web` and validation support in `stem-api`, and Azure Event Hubs client support in `stem-spring-boot-starter` only when Event Hub publishing is enabled

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
