<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>belfius.gejb.stem</groupId>
        <artifactId>stem</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>stem-spring-boot-starter</artifactId>
    <packaging>jar</packaging>

    <name>stem-spring-boot-starter</name>
    <description>Stem Spring Boot Starter - Auto-configuration for the Stem library</description>

    <dependencies>
        <dependency>
            <groupId>belfius.gejb.stem</groupId>
            <artifactId>stem-common</artifactId>
        </dependency>
        <dependency>
            <groupId>belfius.gejb.stem</groupId>
            <artifactId>stem-core</artifactId>
        </dependency>
        <dependency>
            <groupId>belfius.gejb.stem</groupId>
            <artifactId>stem-api</artifactId>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>

        <!-- Spring Boot Auto-configure -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
